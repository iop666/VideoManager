# shared — 两端共享契约

M1 阶段契约以文档为准（`docs/api-contract.md`、`docs/data-model.md`）。

计划（M8 跨平台协同时落地）：

- `api-types.json` — 接口请求/响应 JSON Schema（生成两端类型）
- `models/` — 生成代码：desktop 侧 TypeScript 类型、android 侧 Dart 类
